# script 目录说明（中文）

本目录包含一些 **Shell 脚本**，用于快速运行特定组合实验或论文复现实验。

- 示例：  
  - `demo.sh`：基础 demo；  
  - `run.sh` / `run_com.sh`：批量运行主模型和对比模型；  
  - `run_TSPN_ablation.sh`：TSPN 消融实验脚本；  
  - `LNO_paper/`、`NNSPN_paper/`、`TFON_paper/`：对应论文的专用脚本集合。  

新建大规模实验建议在此目录添加脚本，并在 docs 中记录使用方法。  

