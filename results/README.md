# results 目录说明（中文）

本目录用于存放 **实验结果与汇总表**，一般不直接手动编辑，而由脚本/训练过程生成。

- 例如：  
  - `unified_baseline/`：统一基线实验日志与汇总；  
  - Paper 子项目也有自己的 `Paper/*/results` 目录。  

建议：  
- 统一使用 CSV/JSON 等结构化格式保存结果，方便后续绘图与论文生成。  

